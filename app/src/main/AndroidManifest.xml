<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.learnandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LearnAndroid">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".SubActivity" />
        <activity android:name=".CalcActivity" />
        <activity android:name=".CheckBoxActivity" />
        <activity android:name=".AnimalActivity" />
        <activity android:name=".AndroidVersionActivity" />
        <activity android:name=".Tranning_4Activity" />
        <activity android:name=".MakeLinearLayoutOnJavaCodeActivity" />
        <activity android:name=".CalcTableRowActivity" />
        <activity android:name=".TableLayoutCalcActivity" />
        <activity android:name=".ClockActivity" />
        <activity android:name=".FileWriteActivity" />
        <activity android:name=".DiaryActivity" />
    </application>

</manifest>